@model CityViewModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>City</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
                <br />
                @Html.EditorFor(c => c.Country)

                @*<label style="margin-left: 1em;" asp-for="Country.Id"></label>
                <select class="form-control" asp-for="Country.Id" asp-items='new SelectList( Model.Countries, "Id" , "Name")'></select>
                <span asp-validation-for="Country.Id"></span>*@

            </div>

            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@*


    <label style="margin-left: 1em;" asp-for="CountryIds"></label>
    <select multiple class="form-control" asp-for="CountryIds" asp-items='new MultiSelectList( Model.Countries, "Id" , "Name")'></select>
    <span asp-validation-for="CountryIds"></span>



*@